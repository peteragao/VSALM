# Generated by rstantools.  Do not edit by hand.

# names of stan models
stanmodels <- c("iid_joint_smooth", "iid_joint_smooth_logit", "iid_joint_smooth_unmatched", "iid_mean_smooth", "iid_mean_smooth_unmatched", "spatial_joint_smooth", "spatial_joint_smooth_logit", "spatial_joint_smooth_unmatched", "spatial_mean_smooth", "spatial_mean_smooth_unmatched")

# load each stan module
Rcpp::loadModule("stan_fit4iid_joint_smooth_mod", what = TRUE)
Rcpp::loadModule("stan_fit4iid_joint_smooth_logit_mod", what = TRUE)
Rcpp::loadModule("stan_fit4iid_joint_smooth_unmatched_mod", what = TRUE)
Rcpp::loadModule("stan_fit4iid_mean_smooth_mod", what = TRUE)
Rcpp::loadModule("stan_fit4iid_mean_smooth_unmatched_mod", what = TRUE)
Rcpp::loadModule("stan_fit4spatial_joint_smooth_mod", what = TRUE)
Rcpp::loadModule("stan_fit4spatial_joint_smooth_logit_mod", what = TRUE)
Rcpp::loadModule("stan_fit4spatial_joint_smooth_unmatched_mod", what = TRUE)
Rcpp::loadModule("stan_fit4spatial_mean_smooth_mod", what = TRUE)
Rcpp::loadModule("stan_fit4spatial_mean_smooth_unmatched_mod", what = TRUE)

# instantiate each stanmodel object
stanmodels <- sapply(stanmodels, function(model_name) {
  # create C++ code for stan model
  stan_file <- if(dir.exists("stan")) "stan" else file.path("inst", "stan")
  stan_file <- file.path(stan_file, paste0(model_name, ".stan"))
  stanfit <- rstan::stanc_builder(stan_file,
                                  allow_undefined = TRUE,
                                  obfuscate_model_name = FALSE)
  stanfit$model_cpp <- list(model_cppname = stanfit$model_name,
                            model_cppcode = stanfit$cppcode)
  # create stanmodel object
  methods::new(Class = "stanmodel",
               model_name = stanfit$model_name,
               model_code = stanfit$model_code,
               model_cpp = stanfit$model_cpp,
               mk_cppmodule = function(x) get(paste0("rstantools_model_", model_name)))
})
